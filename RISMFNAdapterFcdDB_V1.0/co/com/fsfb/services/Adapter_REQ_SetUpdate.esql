BROKER SCHEMA co.com.fsfb.services


CREATE COMPUTE MODULE Adapter_REQ_SetUpdate
	CREATE FUNCTION Main() RETURNS BOOLEAN
	BEGIN
		DECLARE refInp REFERENCE TO InputRoot;
		MOVE refInp LASTCHILD;
		MOVE refInp LASTCHILD;
		
		PASSTHRU('UPDATE ADMSALUD.AGFA_MFN SET AGFA_MFN.MFN_01_STATUS = ? WHERE AGFA_MFN.MSH_10_MESS_ID = ?', 'P',refInp.MSH_10_MESS_ID);		
		RETURN FALSE;
	END;
END MODULE;