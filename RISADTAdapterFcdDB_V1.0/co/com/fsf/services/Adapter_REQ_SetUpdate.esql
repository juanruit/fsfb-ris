BROKER SCHEMA co.com.fsf.services


CREATE COMPUTE MODULE Adapter_REQ_SetUpdate
	CREATE FUNCTION Main() RETURNS BOOLEAN
	BEGIN
		DECLARE refInp REFERENCE TO InputRoot;
		MOVE refInp LASTCHILD;
		MOVE refInp LASTCHILD;
		
		PASSTHRU('UPDATE ADMSALUD.AGFA_ADT SET AGFA_ADT.MSH_STATUS = ? WHERE AGFA_ADT.MSH_ID = ?', 'P',refInp.MSH_ID);		
		RETURN FALSE;
	END;
END MODULE;
